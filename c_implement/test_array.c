#include<stdio.h>

int main(void){
   # int i,a[10];
   # for(i=0; i< 10;)
   #     a[i++] = 2*i + 1;
   # for(i=0; i<= 9;i++)
   #     printf("%d", a[i]);
   # printf("\n%d %d\n", a[5.2], a[5.8]);
    int a = 1, b = 2,c;
    c = a^(b<<2)
     
}
