#include<stdio.h>
void main()
{
    
   int i = 0,j = 0, k=0;
   while(str1[i] !="\0" && str2[j]!="\0"){
      if(str1[i] < str2[j])
          str3[k] = str1[i];
      else
          str3[k] = str2[j];
      k++;
   }
   str3[k] = '\0';
   if(str1[] == NULL or str2 == NULL)
       p = str2 +j;
   else
       p = str1 +i;
   strcat(str3,p)
   puts(str3);
}
